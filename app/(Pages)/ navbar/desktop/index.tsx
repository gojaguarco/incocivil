
type Props = {
  className?: string;
}

const DesktopNavBar = ({ className }: Props) => {
  
  return(
    <nav className={`${className} `}>
    Desktop
    </nav>
  )
}

export default DesktopNavBar;